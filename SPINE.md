<!-- Generated by render_spine.py from registry.json. Do not edit by hand. -->

# The RAPP Spine — `rapp-spine/1.0`

> **`registry.json` + `foundation.json` are authoritative.** This file is a
> deterministic human rendering of `registry.json`; `python3 render_spine.py --check`
> fails if it drifts.

KERNEL FIRST: the grail/kernel of record is kody-w/rapp-installer (frozen single-file brainstem, immutable vX.Y.Z tags); kody-w/RAPP is the REFERENCE DISTRO that pins it unmodified + adds userland (rapp-distro/1.0). The rapp-spine is not a new protocol — it is a situational router laid over the full RAPP (Rapid Agent Prototype Platform) protocol stack. It indexes every load-bearing spec as a vertebra in an ordered column — KERNEL -> MAP -> RUNTIME -> DISTRIBUTION -> IDENTITY -> NETWORK -> LEVIATHAN — where each layer answers exactly one question an intelligence faces. To "crawl the spine" is to start from your concrete situation, descend to the layer that owns that concern, and read off the one or few protocols that govern it plus how to act — instead of re-deriving the ~60-repo architecture each time. The spine inherits RAPP's prime directive: every capability rides the single wire (POST /chat, or a signed append-only event), so the router never invents new endpoints — only new agents, cartridges, or §-profiles on top of existing specs. Where concepts share a name, the spine names their relationship explicitly — most importantly the Leviathan stack, where the fleet protocol coordinates Wrapped-Organism beings rather than competing with them.

## How to crawl

- **AI, exhaustive:** fetch `crawl.json`, traverse every ID in `graph.traversal_order`, and read only safe/read-only targets.
- **AI, scoped:** match the concrete situation against `router`, then follow the selected typed route node's exact targets and closure.
- **Human/script:** bare `python3 crawl.py` retains the whole-spine summary; use `python3 crawl.py --full` for exhaustive traversal.
- **Integrity:** run `python3 verify_spine.py --local`.

## Router — situation → protocol

| Situation | Use | Why |
|---|---|---|
| I just want to run AI agents locally, no API keys, fastest path (Tier 1). | rapp-agent/1.0 (kody-w/rapp-installer = grail), rapp-distro/1.0 (kody-w/RAPP = reference distro) | The kernel is the single-file brainstem; the GRAIL of record is kody-w/rapp-installer (current release commit/version 0.6.16). kody-w/RAPP is the reference DISTRO and intentionally pins the older compatible v0.6.0 kernel + userland. GitHub Copilot OAuth means no API keys. |
| I need persistent memory, an always-on cloud body, or a path to Copilot Studio + Teams (Tier 2/3). | CommunityRAPP (Tier-2 Azure Functions), rapp-dataverse/1.0 | CommunityRAPP adds memory on every request and deploys to Azure OpenAI; rapp-dataverse runs the same brainstem natively on Power Platform, ending in a grounded Copilot Studio bot. Both run the agent contract; NOTE function_app.py (T2) emits the kernel envelope (aligned 2026-06-28) (assistant_response/voice_response, no session_id/voice_mode) — aligned to the kernel envelope (2026-06-28) (PARITY.md). |
| I need a brainstem for curl/CI/MCP/scripts with no Flask, no browser, no Copilot subscription. | rapp-brainstem-sdk (vbrainstem_sdk.py) | stdlib-only headless runtime serving the same /chat envelope in real CPython — the headless twin of the browser vBrainstem, ideal for secret/network agents and automation. |
| I want OpenRappter: a consumer-facing, chat-first AI that lives on my machine, hot-loads RAPP agent.py cartridges, and maps RAPP concepts to OpenClaw-compatible experiences. | OpenRappter consumer substrate-distro (RAPP parity target; currently incomplete) | OpenRappter is the full machine-resident RAPP experience and a bidirectional RAPP<->OpenClaw Rosetta stone. Conversation is the control surface: capabilities and configuration are generated through natural language over /chat, while UI views render conversation and state instead of becoming a competing form-driven authority. It may implement the kernel loop with its own architecture, but conformance requires the same /chat behavior and frozen drop-in agent.py ABI. The lineage was running by Groundhog Day 2025, before its first public release; current Python+TypeScript releases have not yet attested RAPP runtime parity. |
| An AI in Claude Desktop / Copilot CLI / Cursor needs to use my local agents or my on-device brainstem. | rapp-mcp-spec/1.0 | MCP is TRANSPORT realizing 'Chat Is The Only Wire' onto /chat — serve a folder as tools, bridge a running brainstem as one tool, or serve a static catalog. It reaches the same cartridge, never a new unit. |
| I need a Rappter to remember everyone across direct and group conversations without leaking confidences, and to honor explicit natural-language consent when a fact may be shared. | rapp-messaging/1.0, rapp-messaging-imessage/1.0 (when the transport is iMessage) | RAPP Messaging defines one global provenance-rich memory graph, per-turn audience projection, familiarity without disclosure, fact-specific consent, group roster epochs, tool authority, and durable inbox/outbox behavior over the existing /chat and signed-event wire. |
| I want to package ONE capability for a nontechnical user who should never see ports or code. | rapp-cart/1.0 | The cartridge is the single abstraction: an agent.py or an .egg, insert -> boot -> run -> eject in RACon. Everything else (ports, twins, registry) stays under the hood. |
| I want to get/share/trade a portable digital twin and have it live locally in ~30s. | rapp-rappid-spec/2.0 (rapp-egg-hub), twin-egg-hatcher | egg-hub ships the twin AS a self-bootstrapping .html (holo card + baked egg + drag-in agent); the generic hatcher materializes any twin/.egg/twin-repo into ~/.rapp/twins/ with no auth. Identity is the self-verifying rappid:<slug>:<64hex>, NO PII. |
| I need an API/registry/catalog/status-badge with zero infrastructure. | rapp-static-api/1.0 | The repo IS the API — CDN-cached, CORS-open, content-addressed fallbacks that never break. It's the foundation under rapp-god, RAR, and rapp-mcp's static catalogs. |
| I must encrypt neighborhood traffic so a relayed channel is as safe as on-device, across languages. | rapp-sealed/1.0 | The canonical AES-256-GCM codec + conformance vectors realizing neighborhood §8, transport-agnostic, key-possession auth. Do NOT confuse with rapp-vneighborhood's separate PIN-seal variant. |
| I'm implementing or documenting ANY neighborhood concern — peer, transport, session, sealed channel, agent-in-a-link. | rapp-neighborhood-protocol/1.0, rapp-twin-chat/1.0 | It's the capstone god-spec owning the vocabulary and the §6 twin-chat envelope every social app rides. Reference the words, don't fork them; mirrors run a CI divergence check. |
| I want an open, cross-estate town square for AIs to broadcast/discover across the whole ecosystem. | rapp-commons-protocol/2.0, rapp-commons-event/1.0 | Stack-agnostic public social network where the key IS the account (rappid) and events are signed append-only. It's an APP/profile on twin-chat — anyone with a keypair joins as an equal. |
| I want my OWN private, brandable, sealed neighborhood (a closed room, not the global square). | rapp-vneighborhood/1.0 | Front-door template: fork, edit neighborhood.json, ship Pages; first twin turns the lights on with PIN+QR. Drop the 'v' and it runs fully offline byte-identically. Caveat: uses a front-door PIN-seal, not rapp-sealed/1.0. |
| My browser-tab neighborhood host isn't durable; I need an always-on relay so late joiners still get in. | rapp-resident | Permanent Azure-Function cloud host serving the same signed rapp-commons-event/1.0 stream over HTTP, trustless (verifies every POST, can refuse but not forge). 'Kited is the floor, this is the ceiling.' |
| I have N projects and want one assistant to fan a question out to a per-project twin and aggregate replies. | rapp-network/1.0 | Project-anchored twins in an on-device neighborhood with dispatch/jobs, offline-first, 127.0.0.1-only, verbatim-NL messages. Each twin answers in its own context; the brainstem aggregates. |
| I want others to discover my whole network of twins from just my GitHub handle. | rapp-estate/1.1, rapp-network-beacon/1.1 | estate.json over pure raw GitHub URLs = 'the network is the backup' (no server/auth); the .well-known beacon advertises indexable/consent + a private-estate pointer. |
| I want to build a serverless holographic social post owned by a keypair. | rapp-moment/1.0 | A Moment is a post + exact UTC instant + 100-frame organism, all static CDN data; an OPTIONAL ECDSA P-256 keypair may sign for ownership (never required). Engine is rapp-hologram. IDENTITY CAVEAT: rapp-moment/1.0 SPEC.md still defers its whole identity model to the RETIRED rapp-eternity/1.0 sha256 content-address — a dangling normative reference. The live rule is rapp/1 §6.2 mint-once (kody-w/rapp-1); the emitted rappid:@owner/slug:64hex string is unchanged, but deriving the tail from content is no longer conformant. NOTE: PolyForm-NC + trademarked marks, unlike the MIT cluster. |
| I run several brainstem nodes and want one mind to coordinate them as a signed fleet over the canonical /chat wire. | leviathan SPEC v1.0 (THE Leviathan PROTOCOL) | ONE external mind drives MANY no-LLM BODIES. CANONICAL wire = rapp-fleet-chat/1.0 (SIGNED twin-chat events over /chat, Art XXV). The legacy POST /api/agent/<name> route is UNAUTHENTICATED — a known Phase-1 RCE, LAN/trusted-subnet ONLY, to be retired. This is the FLEET Leviathan, NOT the being. |
| I want to create/transport ONE operator's full digital being made of many cells across 5 estates. | rapp_leviathan_factory (mint), wrap_leviathan (wire — or leviathan_hub hatch), rapp-leviathan-egg/1.0 (freeze/hatch) | The Wrapped-Organism Leviathan pipeline: intent -> inert 5-estate tree -> live cells -> .egg -> hatch on a new brainstem. This is the OTHER Leviathan (one being, many cells), NOT the fleet protocol. wrap_leviathan is currently unpublished, so leviathan_hub_agent.py `hatch` performs the wiring. |
| I'm lost in the ~60-repo ecosystem, or need to know whether my copy of a load-bearing file is current. | rapp-map (rapp-ecosystem-spec/1.0), rapp-god (rapp-grail-scan/1.0), RAPP-Bible (synthesized narrative), rapp_docs | Crawl the map layer first: rapp-map = WHERE a concept lives; rapp-god = every-version drift observatory + content-addressed fallback frames; RAPP-Bible = synthesized human narrative; rapp_docs = live raw spec text. When the legs disagree, ecosystem-spec.json wins. |
| I want the brainstem framed as a sealed game console with a frozen cartridge contract and a curated starter set. | rappterbox-console-spec/1.0 | A FROZEN additive-only console<->cartridge contract over the same RAPP kernel + Constitution Article XXXIII, with 'Wii Sports' starter carts and on-demand expansion packs. |
| A brainstem on a spotty/high-latency/intermittent link (a 'robot on Mars') must act autonomously and re-aim when it reaches the swarm. | rapp-frame/2.0 | the edge is a brainstem making local judgments; frames/echos re-aim it; degrade-to-one offline. |
| I need the swarm/medium to survive a host being taken down, geoblocked, or banned — no single point of failure. | rapp-hydra/1.0 | static data mirrored to many heads, content-verified; cut one head, read from the next; fork to re-seed. |
| What does the governing law say — is this change allowed, and which discipline/article governs it? | rapp-constitution (kody-w/RAPP/CONSTITUTION.md), MASTER_PLAN (the why-axis; wins ties) | the Constitution is the authoring discipline; the Master Plan is the first-principles north star and wins when they disagree. |
| Where is the canonical, current spec for X — and is my copy authoritative (not drifted)? | RAPP-Bible (canonical aggregate), rapp_docs (live stream), rapp-god (drift detector), foundation.json (locked hashes) | the corpus is aggregated, live-streamed, and drift-watched against canonical hashes; foundation.json locks the foundation pillars + law. |
| What does the kernel/law actually say, or where is a newly-formalized foundation spec? | rapp-kernel/1.0, rapp-canon/1.0, rapp/1, rapp-distro/1.0, rapp-fleet-chat/1.0 | The kernel/governance vertebra: the frozen ABI + the constitution + identity (rapp/1 §6 mint-once, in kody-w/rapp-1 — NOT the retired rapp-eternity/1.0) + the distro model + the canonical fleet wire. Grail=rapp-installer; RAPP is a distro. |
| What did the retired metropolis proposal say about neighborhood/estate aggregation? Or: how does a distro pin the kernel, or fleet talk over /chat? | rapp-metropolis/1.0, rapp-distro/1.0, rapp-fleet-chat/1.0, rapp-runtime-parity/1.0, rapp/1 | The metropolis item is deprecated historical context, pinned to its last self-declaring bytes; it is not a current protocol authority. The live items are the distro pin model, the canonical fleet wire, T1/T2 runtime parity, and rapp/1 §6 mint-once identity (keyless minting keeps the default PKI-free; a keypair is still optional). |
| Auth chain / network boundary / cubby storage / trust+ownership / substrate signing / the three stores / the egg family — where are these governed? | rapp-auth/1.0, rapp-kernel-boundary/1.0, rapp-cubby/1.0, rapp-trust/1.0, rapp-substrate-trust/1.0, rapp-stores/1.0, brainstem-egg/2.3 | The remaining kernel/governance-vertebra specs: the GitHub-Copilot auth chain, the 0.0.0.0/RCE network boundary (Phase-1 open), cubby storage, signing-optional trust, substrate signing, the RAR/RAPP_Store/RAPP_Sense_Store federation, and the egg scale ladder. |
| What is the roadmap / the long-term hardening backlog for the ecosystem? | rapp-roadmap/1.0, rapp-backlog/1.0 | kody-w/rapp-roadmap: the grounded 5-phase roadmap (re-anchored on the metropolis north star) + the adversarial BACKLOG (165 wrenches). How we build from here without unlocking the kernel. |
| I want to PACKAGE/PUBLISH a rapplication bundle or a sense overlay, or handle a holocard; or reach a dark-door neighborhood by dialing a rappid. | rapp-application/1.0, rapp-sense/1.0, rappcards-binder/1.0, rapp-demo/1.0, rapp-payphone-dial/1.0 | The distribution/network sub-protocols: the rapplication bundle FORMAT (RAPP_Store), the sense overlay FORMAT (RAPP_Sense_Store), the holocard format (RAPPcards), the demo bundle, and the payphone/dial async-reach pattern (a sub-protocol of rapp-neighborhood-protocol). |
| Where do I PUBLISH or DISCOVER the canonical agent registry (a bare agent.py)? | rapp-registry/1.0 | kody-w/RAR is THE canonical home + minting authority for bare agents (the 'RAR + RAPP_Store registry path'): RAR for agents, RAPP_Store for rapplications, RAPP_Sense_Store for senses. |
| How do I make the brainstem + its organs survive reboot, and see/control which organs are running? | rapp-burrow/1.0 | The Burrow organ-rapplication supplies launchd persistence and a locally-served PWA control room. Its current organ aggregation uses the legacy /api/agent surface and is non-conformant until migrated to the canonical /chat wire. |

## Column — kernel · map · runtime · distribution · identity · network · leviathan

### kernel

The kernel + its governance: the frozen single-file brainstem (rapp-agent/1.0, GRAIL=kody-w/rapp-installer; kody-w/RAPP is the reference distro pinning it via rapp-distro/1.0). Specs: rapp-kernel/1.0, rapp-auth/1.0, rapp-kernel-boundary/1.0, rapp-runtime-parity/1.0, rapp-canon/1.0, rapp-cubby/1.0, rapp-trust/1.0, rapp-substrate-trust/1.0, rapp-distro/1.0 (distros), rapp-fleet-chat/1.0 (fleet over /chat), rapp-stores/1.0, brainstem-egg/2.3. The retired rapp-metropolis/1.0 proposal remains indexed only as pinned historical material. The kernel never breaks userspace.

**Protocols:** brainstem-egg/2.3, rapp-auth/1.0, rapp-backlog/1.0, rapp-canon/1.0, rapp-cubby/1.0, rapp-distro/1.0, rapp-fleet-chat/1.0, rapp-kernel-boundary/1.0, rapp-kernel/1.0, rapp-metropolis/1.0, rapp-roadmap/1.0, rapp-runtime-parity/1.0, rapp-stores/1.0, rapp-substrate-trust/1.0, rapp-trust/1.0

### map (orientation + observability)

Crawl here FIRST when lost or auditing. Answers 'where does this concept live?' and 'is my copy current?'. rapp-map indexes WHICH repo owns WHICH spec; rapp-god is the live every-version content-addressed registry + drift observatory (observes, never fixes); RAPP-Bible is the synthesized human narrative ('the one file to read'); rapp_docs streams every spec's raw text live with zero copies. These four are the 'drift triangle' (actually 4 legs) anchored on ecosystem-spec.json — when they disagree, the JSON wins.

**Protocols:** rapp-ecosystem-spec/1.0 (rapp-map), rapp-grail-scan/1.0 (rapp-god), rapp/1 (identity authority — kody-w/rapp-1 SPEC.md §6; RAPP-Bible mirrors the RETIRED rapp-eternity/1.0 narrative, which is history, not current law), rapp_docs (live renderer, no slug)

### runtime (the engine)

What actually runs single-file *_agent.py cartridges through one /chat tool-calling loop. The reference kernel is rapp-agent/1.0 in kody-w/rapp-installer; substrate-distros may implement the same locked loop and frozen agent ABI on different machinery. CommunityRAPP, rapp-brainstem-sdk, rappterbox, and rapp-dataverse occupy that family. OpenRappter is the intended consumer-facing machine AI and RAPP<->OpenClaw Rosetta stone; its current Python+TypeScript release has not yet proved runtime parity, so it remains a conformance target rather than a second kernel.

**Protocols:** rapp-agent/1.0 (grail: rapp-installer), rappterbox-console-spec/1.0, rapp-dataverse/1.0, CommunityRAPP (Tier-2, no slug), rapp-brainstem-sdk (no slug), openrappter (consumer substrate-distro; parity target incomplete)

### distribution (package, name, ship, install)

How a unit becomes shippable. The single user-facing abstraction is the cartridge (rapp-cart/1.0): if it's an agent.py or an .egg, drop it in RACon and it runs. Twins/eggs ship via rapp-egg-hub (rapp-rappid-spec/2.0) and materialize via the generic twin-egg-hatcher; rapp-agents is a flat community stack + loader; rapp-static-api/1.0 is the zero-server 'the repo IS the API' convention every catalog builds on; the sacred one-liner installers (rapp-installer), planted public instances (heimdall, conforming to the Mirror Spec), and the Wrapped-Organism egg hub (rapp-leviathan-egg/1.0) live here too. RAPP_Hub is archived -> RAR + RAPP_Store registry path.

**Protocols:** rapp-cart/1.0, rapp-rappid-spec/2.0 (rapp-egg-hub), rapp-static-api/1.0, rapp-leviathan-egg/1.0 (rapp-leviathan-hub), rapp-installer (sacred, no slug), twin-egg-hatcher (tool), rapp-agents (content), heimdall (Mirror-Spec instance), RAPP_Hub (DEPRECATED)

### identity (who an agent IS + confidentiality)

Cryptographic identity, ownership, and sealing. rapp-sealed/1.0 is the canonical AES-256-GCM sealed-envelope codec with cross-language conformance vectors (auth by key-possession only; transport-agnostic; realizes neighborhood §8). rapp-moment/1.0 is a keypair-signed (ECDSA P-256) social primitive that uses an OPTIONAL keypair binding. CANONICALLY identity is rapp/1 §6, in kody-w/rapp-1 — SPEC.md pinned at 6723c7add2aed36bb68992fc71a56b0a4bd5ad81 (41880 bytes, sha256 6d06daba65d7c045716f3d6e95db8401ab58e727820e4114466d847f62cae49b). §6.1 leaves the STRING untouched: rappid:@owner/slug:64hex is still the only conformant form, so nothing that merely EMITS a rappid is broken. §6.2 changed MINTING: the 64-hex tail is minted exactly ONCE and is then immutable — keyless Hb('rapp/1:rappid', uuid4_octets) or keyed Hb('rapp/1:rappid', SPKI_DER) — and a producer MUST NOT derive the tail from owner/slug or any name. Keys stay OPTIONAL (keyless minting is a first-class branch), so gh-collaborator remains the default ownership and a keypair remains sovereignty, NEVER a requirement. rapp-eternity/1.0 — which taught the sha256 CONTENT-ADDRESS tail — is RETIRED as of 2026-07-17 (kody-w/rapp-eternity commit 9c970a1e; its SPEC.md is now a tombstone reading 'RETIRED · SUBORDINATE TO RAPP · NON-NORMATIVE'). Read it as history; never cite it as current or as a compatibility authority. Anything still COMPUTING a tail by hashing its own content is now non-conformant even though the emitted string still looks right.

**Protocols:** rapp/1 (canonical identity — §6 of kody-w/rapp-1 SPEC.md; also owns canonicalization, content addressing, the frame, the wire and the egg), rapp-sealed/1.0, rapp-moment/1.0, rapp-eternity/1.0 (RETIRED 2026-07-17 — historical record only)

### network (peers, transports, neighborhoods, federation)

Once you have a runtime + identity, how organisms find and talk to each other. The capstone god-spec is rapp-neighborhood-protocol/1.0 — it owns the vocabulary and the rapp-twin-chat/1.0 §6 envelope (delegating the §8 seal to rapp-sealed). rapp-messaging/1.0 profiles human trust, memory projection, consent, group epochs, and delivery over that existing wire. Everything else is a §5 transport or a §-profile carrying that same envelope, never a new peer type: rapp-commons-protocol/2.0 (open global town square), rapp-vneighborhood/1.0 (private brandable front-door template), rapp-network/1.0 (project-twin fan-out), rapp-estate/1.1 (operator-wide discovery, 'the network is the backup'), rapp-mcp-spec/1.0 (MCP host onto /chat), rapp-kite ('the string' CDP operator tools), rapp-resident (always-on Azure cloud relay; 'kited is the floor, this is the ceiling').

**Protocols:** rapp-neighborhood-protocol/1.0 (owns rapp-twin-chat/1.0 §6), rapp-messaging/1.0 (+ rapp-messaging-imessage/1.0), rapp-commons-protocol/2.0 (+ rapp-commons-event/1.0), rapp-vneighborhood/1.0, rapp-network/1.0, rapp-estate/1.1 (+ rapp-network-beacon/1.1), rapp-mcp-spec/1.0, rapp-kite (operator tooling), rapp-resident (cloud relay)

### leviathan (multicellular beings — COLLISION LAYER)

Many parts acting as one organism. The two senses STACK (protocol builds on the being-medium). (A) The Leviathan PROTOCOL coordinates many brainstem bodies; its canonical wire is rapp-fleet-chat/1.0 signed twin-chat over /chat. The legacy injected POST /api/agent/<name> implementation remains a known Phase-1 RCE and is non-conformant. (B) The Wrapped-Organism Leviathan (rapp_leviathan_factory -> wrap_leviathan -> rapp-leviathan-hub) is one operator's digital being built of many cells across 5 estates (Sanctum/Polity/Works/Press/Commons = soul/will/hands/eyes/mouth). The cell-protocol underpinning (B) is the Wrapped Organism Spec, and its status was re-checked on 2026-08-04: it is UNPUBLISHED, not missing. kody-w/rappterbook's default branch tracks neither WRAPPED_ORGANISM_SPEC.md nor scripts/wrapped_organism/ (zero matching tracked paths on main), but both exist on that same repo's unmerged branch worktree-wrapped-organism — docs/WRAPPED_ORGANISM_SPEC.md fetches HTTP 200, 29891 bytes, sha256 9c9674bab36de2be… at pinned tip 51636f596070. The repo attribution is therefore sound (rappterbook's main really does own EGG_SPEC.md); what is wrong is calling the spec 404. The standalone spec repo kody-w/rappter also exists — it is PRIVATE (created 2026-03-10, last push 2026-05-17), which an anonymous crawl reports as 404. Both are visibility/merge gaps, not lost documents.

**Protocols:** leviathan SPEC v1.0 — FLEET protocol: one mind, many signed /chat BODIES, rapp-leviathan-egg/1.0 — BEING egg (distribution-layer hub), rapp_leviathan_factory (mint inert 5-estate tree; agent in RAR), wrap_leviathan (wire stage — UNPUBLISHED/404), WRAPPED_ORGANISM_SPEC.md — UNPUBLISHED: kody-w/rappterbook branch worktree-wrapped-organism only, not on main; no document slug, defines the cell manifest wire schema rapp-cell/1.0

## Registry — every vertebra

| Spec | Repository | Layer | Purpose / when to use | Entry point |
|---|---|---|---|---|
| `rapp-agent/1.0` | kody-w/RAPP | runtime | Default Tier-1 install — the reference distro. It ships an UNMODIFIED pinned kernel (v0.6.0) plus userland. For the canonical kernel itself, see kody-w/rapp-installer (the grail). | curl -fsSL https://kody-w.github.io/RAPP/installer/install.sh \| bash (global ~/.brainstem :7071; add `-s -- --here` for project-local :7072+) |
| `(none; Tier-2 Azure-Functions runtime)` | kody-w/CommunityRAPP | runtime | When you need persistent memory, an always-on cloud body, or a ramp to Copilot Studio + Teams. hatch-project scaffolds an isolated per-project RAPP. | curl -fsSL https://raw.githubusercontent.com/kody-w/CommunityRAPP/main/install.sh \| bash (scaffold: .../hatch-project.sh \| bash -s -- my-project) |
| `(none; implements brainstem /chat envelope)` | kody-w/rapp-brainstem-sdk | runtime | When you need a brainstem with NO Flask, NO browser, NO Copilot-subscription gate — for curl/CI/MCP/script callers, or to run secret/network agents in real CPython. | python3 vbrainstem_sdk.py serve --port 7173 (also: run @kody-w/<agent> '...', agents --grep, eval) |
| `openrappter-runtime/1.0` | kody-w/openrappter | runtime | When you want the full consumer AI living on a machine — daemon, desktop/web UI, channels, memory, and OpenClaw interoperability — while retaining RAPP cartridge compatibility. Its runtime contract is published as openrappter-runtime/1.0 in SPEC.md; it has not been independently assessed against the reference kernel. | curl -fsSL https://kody-w.github.io/openrappter/install.sh \| bash (or hand skills.md to any AI agent to self-install) |
| `rappterbox-console-spec/1.0` | kody-w/rappterbox | runtime | When you want the brainstem framed as a game console with a locked cartridge contract and 'Wii Sports' starter carts (ManageMemory, ContextMemory, HackerNews, LearnNewAgent). | curl -fsSL https://kody-w.github.io/rappterbox/installer/install.sh \| bash (then bash ~/.brainstem/start.sh; --with twin) |
| `rapp-agent/1.0 (the kernel — the grail of record)` | kody-w/rapp-installer | distribution | Legacy/teaching path. THE grail/kernel of record — the one-liner installs from it. kody-w/RAPP is a downstream DISTRO that pins the unmodified kernel (rapp-distro/1.0). | curl -fsSL https://kody-w.github.io/rapp-installer/install.sh \| bash (Tier 2: .../community_rapp/install.sh) |
| `leviathan SPEC v1.0 (self-titled; no rapp-* slug)` | kody-w/leviathan | leviathan | You run several brainstem nodes and want one mind to coordinate them through signed fleet-chat events over the canonical /chat boundary. The old direct no-LLM route is a documented RCE and is not the conformant path. | CANONICAL: use rapp-fleet-chat/1.0 signed twin-chat events over /chat (read FLEET_CHAT.md). LEGACY ONLY: flock_endpoint.py injects unauthenticated /api/agent and is non-conformant; do not expose or deploy it as the canonical fleet wire. |
| `rapp-leviathan-egg/1.0` | kody-w/rapp-leviathan-hub | distribution | Transport/share a whole 5-estate digital being across machines/operators, or grab a ready-made one (eggs: kody 92 cells / macrohard 154 cells) and talk to it locally. | Install leviathan_hub_agent.py into agents/, then /chat: 'Use LeviathanHub with action=hatch, egg=kody'; talk via the auto-wired Ask<Slug> tool. Actions: hub_status/list_eggs/download/hatch/freeze. |
| `(none; agent v0.2.0 lives in RAR, standalone repo 404s)` | kody-w/rapp_leviathan_factory | leviathan | Stage 1: generate a brand-new digital being from intent (optionally a 1-4 estate subset) before wiring it live or freezing it to an egg. | Drop kody-w/RAR/agents/@kody-w/rapp_leviathan_factory_agent.py into agents/, then RappLeviathanFactory(action='generate', intent='...', name='kody', estates=[1,2,3,4,5]). |
| `(none; UNPUBLISHED / 404 — gap)` | kody-w/wrap_leviathan | leviathan | Aspirational. Its wiring role is currently performed by leviathan_hub_agent.py `hatch` (the retrofit that writes per-cell agent.py + a brainstem shim). Hub explicitly works without it. | NOT FOUND / 404. Use leviathan_hub hatch instead. |
| `(WRAPPED_ORGANISM_SPEC.md — self-titled 'The Wrapped Organism, RAPP Cell Architecture' v1.0; no document-level slug; UNPUBLISHED — branch-only)` | kody-w/rappterbook | leviathan | When you need the canonical Wrapped Organism / cell-protocol definition. It is readable today, but only off the default branch: docs/WRAPPED_ORGANISM_SPEC.md on kody-w/rappterbook@worktree-wrapped-organism (HTTP 200, 29891 bytes, sha256 9c9674bab36de2bee7a9bbead76e2384f58d9aab5de17499c1cda24d9d9a0abb at the pinned tip). Treat it as UNPUBLISHED until that branch merges — the spine deliberately records no raw_url for it, because a branch that has never reached main is not a canonical publication and must not be pinned as one. | read the UNPUBLISHED branch pin https://raw.githubusercontent.com/kody-w/rappterbook/51636f596070630b0aab81fc4c7b1855850e9f0d/docs/WRAPPED_ORGANISM_SPEC.md . Reference cell runtime on the same branch: scripts/wrapped_organism/ (__init__.py, cell.py, retrofit.py) plus tests/test_wrapped_organism.py. None of these paths exist on main. |
| `rapp-static-api/1.0` | kody-w/rapp-static-apis | distribution | When you need a registry/catalog/status endpoint with zero infrastructure: CDN-cached, CORS-open, forkable, auditable in git, with pinnable content-addressed fallbacks. Reference impls: rapp-god (full), RAR (index), rapp-mcp (profile). | Copy template/ into a new repo, edit manifest.json, run python3 build.py -> registry.json + api/v1/*.json. SPEC.md in repo. |
| `rapp-mcp-spec/1.0` | kody-w/rapp-mcp | network | When an AI in Claude Desktop / Copilot CLI / Cursor needs to use the user's local agents or on-device brainstem, or to distribute a static content-verified catalog. | python3 rapp_mcp.py /path/to/agents \| python3 rapp_brainstem_mcp.py (bridge; RAPP_BRAINSTEM_URL default http://localhost:7071) \| python3 rapp_static_mcp.py <tools.json-url> |
| `rapp-rappid-spec/2.0` | kody-w/rapp-egg-hub | distribution | When you want to get/trade/contribute a portable digital twin and have it live locally in ~30s, or author a twin against the frozen identity spec. | Gallery https://kody-w.github.io/rapp-egg-hub/ -> open a twin .html -> Get -> drag agent.py into agents/. |
| `(none; generic tool — public mirror of private aibast-twin)` | kody-w/twin-egg-hatcher | distribution | When you have a twin source (public/private repo or exported .egg) and want to instantiate it locally with no auth, then federate it back to the brainstem's built-in Twin agent. Requires a brainstem first. | curl -fsSL https://raw.githubusercontent.com/kody-w/twin-egg-hatcher/main/install.sh \| bash (drops twin_egg_hatcher_agent.py / HatchTwinEgg tool) |
| `rapp-cart/1.0` | kody-w/rapp-carts | distribution | When defining/installing a RAPP rapplication as ONE user-facing unit, especially for nontechnical users who should only see insert -> boot -> run -> eject. | SPEC.md in repo. Surfaces: RACon (console), vRACon (browser/Pyodide), RACon Kited (cross-device). brainstem.py = the bootloader. |
| `(none; content — public agent stack)` | kody-w/rapp-agents | distribution | When you want ready-made community agents or a single front-door loader to manage agents/bundles by chatting — without touching the sacred kernel. Simpler flat stack vs RAR's content-addressed registry. | Download agents/rapp_loader_agent.py into your brainstem's agents/ dir. MIT. |
| `rapp-sealed/1.0` | kody-w/rapp-sealed | identity | When any caller (browser vBrainstem, bridge, CLI, Node, MCP host) must encrypt/decrypt neighborhood traffic so a relayed channel is as secure as on-device, with cross-language guarantees. | import { seal, open } from './rapp_sealed.mjs' (Web Crypto, no deps); self-check node verify.mjs against test-vectors.json. MIT. |
| `rapp-moment/1.0` | kody-w/rapp-moment | identity | When building an interoperable backend-free client for the holographic social network (mint/play/own/embed Moments), or you need the normative wire format + conformance rules. | SPEC.md + CONFORMANCE.md in repo. Reference engine: kody-w/rapp-hologram. Play: ?m=<base64url(JSON)>; dial: ?dial=<pk>. License: PolyForm-NC 1.0.0 + trademarked marks (NOT MIT). |
| `rapp-dataverse/1.0` | kody-w/rapp-dataverse | runtime | When you want to run/teach/compare RAPP natively in Power Platform (ending in a grounded Copilot Studio bot) without a Dataverse environment, or explore via a no-setup public vTwin. | SPEC.md in repo. CLI: python3 rapp_dv.py whoami\|agents list\|chat '...'\|hatch\|compare. Live: https://kody-w.github.io/rapp-dataverse/chat/. MIT. |
| `rapp-neighborhood-protocol/1.0 (owns rapp-twin-chat/1.0)` | kody-w/rapp-neighborhood-protocol | network | When implementing/documenting ANY neighborhood concern — peer, transport (§5), sealed channel (§8), session (§9), multiplayer (§10), agent-in-a-link (§15), controller (§17). Reference the words, don't fork them. | NEIGHBORHOOD_PROTOCOL.md in repo. Reference impl: kody-w/vbrainstem (§12); realized by twin_chat_agent.py in rapp-commons (§17). MIT. |
| `rapp-messaging/1.0 (+ rapp-messaging-imessage/1.0)` | kody-w/rapp-messaging | network | When a Rappter talks with multiple people or groups and must retain full local context while deciding what is appropriate to recall or disclose in each conversation, including consent changes made naturally mid-thread. | Read SPEC.md and IMESSAGE.md; machine schemas live under schemas/, conformance vectors under vectors/, and OpenRappter PR #28 is the reference implementation. |
| `(none; operator tooling — 'the string')` | kody-w/rapp-kite | network | When an operator (canonically Claude) must be the connective tissue between a public/browser peer and a machine's LOCAL brainstem — join a hosted vBrainstem from a shell, hold the CDP string to a local tab, tether a deployed page to a local brainstem. | Clone + run, e.g. vbridge.sh <peer> <token> <ask\|eval\|run\|chat\|agents\|health>. CDP hop stays on-machine; only cross-machine rides WebRTC + seal. MIT. |
| `rapp-ecosystem-spec/1.0 (byte-identical mirror)` | kody-w/rapp-map | map | When lost in the ~60-repo ecosystem and you need to know WHERE a concept lives, or to wire a new repo into its correct neighborhood without creating drift. | https://github.com/kody-w/rapp-map (README = human map; ecosystem-spec.json = machine map). Tagged v1.0.0. |
| `rapp-grail-scan/1.0` | kody-w/rapp-god | map | When you need to know if your copy of a load-bearing file (install.sh, kernel, spec, codec) is current vs the grail, pin/fetch an exact prior version as a fallback, or audit cross-repo drift. Registers the ECOSYSTEM (vs RAR, which registers agents). | https://kody-w.github.io/rapp-god/ (dashboard); registry.json + api/v1/status.json (static API); build_god.py. |
| `(none; live spec renderer)` | kody-w/rapp_docs | map | When you want the current text of any RAPP spec (Moment, Eternity, ecosystem-spec, Neighborhood, static-apis, mcp, dataverse, GRAIL_SCAN, Bible docs) without hunting across repos. | https://kody-w.github.io/rapp_docs/ (specs.json is the index; add a spec = add one raw URL line). |
| `(DEPRECATED -> the RAR/RAPP_Store registry path)` | kody-w/RAPP_Hub | distribution | Effectively never — historical reference only. For the live catalog go to the RAR/RAPP_Store registry path. | MOVED -> the RAR + RAPP_Store registry path (live: https://kody-w.github.io/RAPP_Store/). |
| `rapp-estate/1.1 (canonical def in kody-w/RAPP specs/SPEC.md)` | kody-w/rapp-estate | network | When you want to discover/walk an operator's whole network of organisms from just their GitHub handle, or become a 1st-class citizen by publishing your own estate. | curl raw.githubusercontent.com/<you>/rapp-estate/main/estate.json; rebuild via tools/rebuild_estate.py --handle <you>; discover via gh search repos topic:rapp-estate. |
| `rapp-network/1.0 (Draft v0)` | kody-w/RAPP-Network | network | When you have N projects each with its own context and want one assistant to fan a question out to per-project twins, let each answer in its own context, and aggregate the replies. | cp project_twin_agent.py into agents/, then drive in English: hatch/list/boot/chat/dispatch/job_status/await_job/stop. SPEC.md in repo. |
| `rapp-commons-protocol/2.0 (+ rapp-commons-event/1.0)` | kody-w/rapp-commons | network | When you want an open cross-estate 'town square' for agents requiring no particular stack — anyone with a keypair joins as an equal. Reach for it over a private project neighborhood to broadcast/discover across the whole ecosystem. | curl swarm_agent.py && python3 swarm_agent.py (mint rappid + kite in; say/read --room). Live: https://kody-w.github.io/rapp-commons/. Graduates to cloud via rapp-resident. |
| `rapp-vneighborhood/1.0` | kody-w/rapp-vneighborhood | network | When you want your OWN private, brandable, sealed closed room (own focus/kinds/rules/accent) — fork the template, edit neighborhood.json, ship Pages. Reach for it INSTEAD of rapp-commons when you need a closed room, not the open global square. | Use template -> Settings>Pages, edit neighborhood.json. Live: https://kody-w.github.io/rapp-vneighborhood/. CAVEAT: its PIN-seal is a front-door variant, NOT canonical rapp-sealed/1.0. |
| `(none; Mirror-Spec instance, kernel-derived (one-string change; NOT byte-identical) to grail)` | kody-w/heimdall | distribution | When you want a concrete public reference of the plant/mirror pattern — visit it, install ITS brainstem (a thin wrapper re-fetching the canonical grail installer so it can't drift), or hatch it as a twin. A worked example, not a protocol. | Visit https://kody-w.github.io/heimdall; install: curl -fsSL https://kody-w.github.io/heimdall/installer/install.sh \| bash; hatch via twin-egg-hatcher --source kody-w/heimdall. |
| `(none; serves rapp-commons-event/1.0 over HTTP)` | kody-w/rapp-resident | network | When a browser-tab host isn't durable enough and you want a vneighborhood (the Commons, a forum, your room) to 'graduate' to an always-on relay so late joiners still get in. 'Kited is the floor, this is the ceiling.' | az login && func --version (v4) && ./deploy.sh; add base URL to neighborhood.json -> commons.cloud_hosts. Routes: /api/health, /api/rooms/{room}/events. Live: rapp-resident-kw165843.azurewebsites.net/api. |
| `rapp-frame/2.0` | kody-w/rapp-frame-net | leviathan | a brainstem on a spotty/high-latency link must act autonomously and re-aim when it reaches the swarm | drop edge_node_agent.py into a brainstem; raw.githubusercontent.com/kody-w/rapp-frame-net/main/net/latest.json |
| `rapp-hydra/1.0` | kody-w/rapp-frame-net | network | I must keep reading the swarm even if a host/CDN is taken down, geoblocked, or bans the account | an edge head-list with content-verify + failover; HYDRA.md |
| `rapp-sense/1.0` | kody-w/RAPP_Sense_Store | distribution | Canonical public catalog + spec for RAPP senses (modular per-channel output overlays that drop into a brainstem's utils/senses/); peer to RAR (agents) and RAPP_Store (rapplications) | Browse index.json / submit via @rapp/rapp_publish_agent or a [SENSE] @<user>/<slug> issue; install sense .py files into rapp_brainstem/utils/senses/ |
| `rapp-application/1.0` | kody-w/RAPP_Store | distribution | Public catalog + canonical spec for rapplications (bundled single-file agent + UI/state/service/twin cartridges); defines bundle layout, manifest schema, gated-rapp federation, and the static Pokedex API. | Browse the static Pokedex API at https://raw.githubusercontent.com/kody-w/RAPP_Store/main/api/v1/index.json (or the viewer kody-w.github.io/RAPP_Store/); rapps conform to rapp-application/1.0 and drop into a local brainstem. |
| `rappcards-binder/1.0` | kody-w/RAPPcards | distribution | Canonical owner of the portable RAPPcards trading-card standard (card data model, deterministic seed identity, incantation protocol, lossless export, cross-binder federation) — declares rappcards-binder/1.0 + rappcards-peers/1.0 + rappcards-seed-index/1.0 | Read SPEC.md for the standard; live site at kody-w.github.io/RAPPcards (index.html browse, binder.html local-first IndexedDB collection). Federation bootstrap: raw.githubusercontent.com/kody-w/RAPPcards/main/peers.json |
| `rapp-demo/1.0` | kody-w/rapp-demos | distribution | Owner of the rapp-demo/1.0 scenario schema and its scan-to-watch synced demo player: an M365-style template where a host walks a text scenario step-by-step and QR/link watchers follow live, read-only, over a rapp-sealed WebRTC channel. | Live player at https://kody-w.github.io/rapp-demos/ ; author/import a `rapp-demo/1.0` scenario JSON (see scenarios/) into the fixed M365 template and drive it; schema canonically defined inline in README.md |
| `rapp-spine/1.0` | kody-w/rapp-spine | map | The situational router/index over the full RAPP protocol stack — maps situation→layer→protocol across ~60 repos and ~33 specs; also owns FOUNDATION (foundation.json, the locked content-addressed canonical registry) and the crawlable registry.json | Crawl it: fetch https://raw.githubusercontent.com/kody-w/rapp-spine/main/registry.json and match your situation against `router`, or run `python crawl.py "<situation>"`; full instructions in CRAWL.md. Read SPINE.md for the whole column + collisions. |
| `rapp/1` | kody-w/rapp-1 | identity | Whenever you mint, read, canonicalize or verify a rappid — §6 is the CANONICAL identity authority for the estate, in place of the RETIRED rapp-eternity/1.0. Emitters of rappid:@owner/slug:64hex are unaffected, because §6.1 keeps that grammar verbatim; MINTERS that hash their own content to build the 64-hex tail no longer conform and must move to §6.2 mint-once. Also the place to look for the frame, the wire, the egg and content addressing. | read SPEC.md §6 in kody-w/rapp-1. Pinned authority: kody-w/rapp-1@6723c7add2aed36bb68992fc71a56b0a4bd5ad81 — SPEC.md, 41880 bytes, sha256 6d06daba65d7c045716f3d6e95db8401ab58e727820e4114466d847f62cae49b (the same pin rapp-map/RAPP1_AUTHORITY.json records; structural pin only). |
| `rapp-eternity/1.0` | kody-w/rapp-eternity | identity | rapp-eternity/1.0 — RETIRED historical identity standard, superseded by rapp/1 §6 in kody-w/rapp-1. Retired 2026-07-17 (commit 9c970a1e, then 17bb6d21); SPEC.md is now a tombstone declaring itself 'RETIRED · SUBORDINATE TO RAPP · NON-NORMATIVE'. Its content-address minting rule and its estate-wide identity authority are both withdrawn. Read for history; do not implement it, cite it as current, or treat it as a compatibility authority. | read SPEC.md in kody-w/rapp-eternity for the historical record only — the live identity authority is SPEC.md §6 in kody-w/rapp-1 |
| `rapp-fleet-chat/1.0` | kody-w/leviathan | kernel | RAPP Fleet-Chat — `rapp-fleet-chat/1.0` | read FLEET_CHAT.md in kody-w/leviathan |
| `rapp-kernel/1.0` | kody-w/rapp-spine | kernel | rapp-kernel/1.0 | read specs/KERNEL.md in kody-w/rapp-spine |
| `rapp-metropolis/1.0` | kody-w/rapp-estate | kernel | rapp-metropolis/1.0 — RETIRED and DEPRECATED historical mesh-composition proposal. The live main/METROPOLIS.md path is a retirement notice and explicitly publishes no protocol authority; these immutable baseline bytes are retained for audit and history only. | read the pinned historical METROPOLIS.md only to audit the retired proposal; do not implement it as current |
| `rapp-auth/1.0` | kody-w/rapp-spine | kernel | rapp-auth/1.0 — The Brainstem Auth Chain | read specs/AUTH_CHAIN.md in kody-w/rapp-spine |
| `rapp-kernel-boundary/1.0` | kody-w/rapp-spine | kernel | rapp-kernel-boundary/1.0 — The Kernel Network Trust Boundary | read specs/NETWORK_TRUST_BOUNDARY.md in kody-w/rapp-spine |
| `rapp-trust/1.0` | kody-w/rapp-spine | kernel | Trust & ownership — defers identity to rapp/1 §6 (mint-once; keyless or keyed; keypair still optional). Its three-layer model (L0 keyless identity / L1 gh-collaborator default / L2 optional keypair) is unaffected, but the rapp-eternity/1.0 content-address minting rule it quoted is retired. | read specs/TRUST_AND_OWNERSHIP.md in kody-w/rapp-spine |
| `rapp-substrate-trust/1.0` | kody-w/rapp-spine | kernel | The Substrate Trust Model — `rapp-substrate-trust/1.0`. Consumes identity only; its identity deferral now reads rapp/1 §6, not the retired rapp-eternity/1.0. | read specs/SUBSTRATE_TRUST.md in kody-w/rapp-spine |
| `rapp-cubby/1.0` | kody-w/rapp-spine | kernel | The Cubby Shelf & the Super-RAR | read specs/CUBBY.md in kody-w/rapp-spine |
| `rapp-stores/1.0` | kody-w/RAPP_Store | kernel | RAPP Stores — The Three-Store Topology | read specs/STORES.md in kody-w/RAPP_Store |
| `brainstem-egg/2.3` | kody-w/rapp-egg-hub | kernel | The `.egg` Cartridge Family & Multi-Scale Hatch Contract | read EGG_FAMILY.md in kody-w/rapp-egg-hub |
| `rapp-payphone-dial/1.0` | kody-w/rapp-neighborhood-protocol | network | PAYPHONE.md | read PAYPHONE.md in kody-w/rapp-neighborhood-protocol |
| `rapp-runtime-parity/1.0` | kody-w/rapp-spine | kernel | rapp-runtime-parity/1.0 | read specs/PARITY.md in kody-w/rapp-spine |
| `rapp-canon/1.0` | kody-w/rapp-spine | kernel | rapp-canon/1.0 — see kody-w/rapp-spine | read specs/CANON.md in kody-w/rapp-spine |
| `rapp-distro/1.0` | kody-w/rapp-distro | kernel | The RAPP distro standard — a distro = the unmodified pinned grail kernel + a userland; spawn permissionlessly; freeze CI proves the kernel is unmodified. Grail=rapp-installer; RAPP is the reference distro. | read SPEC.md in kody-w/rapp-distro; spawn-distro.sh scaffolds a distro |
| `rapp-roadmap/1.0` | kody-w/rapp-roadmap | kernel | The grounded 5-phase ecosystem roadmap (re-anchored on the metropolis north star). | read in kody-w/rapp-roadmap |
| `rapp-backlog/1.0` | kody-w/rapp-roadmap | kernel | The adversarial hardening backlog (165 wrenches + pre-solves). | read in kody-w/rapp-roadmap |
| `rapp-registry/1.0` | kody-w/RAR | distribution | The RAPP Agent Registry — canonical home + minting authority for every agent (SDK validate->stage->approve->forge->registry.json); peer to RAPP_Store/RAPP_Sense_Store. | submit via the RAR SDK; browse registry.json |
| `rapp-burrow/1.0` | kody-w/rapp-burrow | distribution | The ORGAN-RAPPLICATION pattern: a drop-in organ that burrows the brainstem into launchd (persistence for the brainstem + every organ) and serves a locally-exposed PWA pulled from this global repo — the Organ Control Room (operator visibility + enable/disable + observe/live over persistent/monitoring organs). | read SPEC.md in kody-w/rapp-burrow; drop burrow_agent.py into any brainstem agents/ |

## Collisions and gaps

- LEVIATHAN — the two senses STACK (protocol builds on the being-medium) (the load-bearing collision). (A) Leviathan PROTOCOL (kody-w/leviathan, self-titled SPEC v1.0): ONE external mind coordinates MANY brainstem BODIES; canonical wire = rapp-fleet-chat/1.0 signed twin-chat over /chat. The old injected POST /api/agent route is a non-conformant RCE, not the protocol's future. (B) Wrapped-Organism Leviathan (rapp_leviathan_factory -> wrap_leviathan -> rapp-leviathan-hub/rapp-leviathan-egg/1.0): ONE operator's single digital BEING built of MANY CELLS across 5 estates (Sanctum/Polity/Works/Press/Commons); 'one being, many cells.' NOT orthogonal — a STACK (protocol builds on the being-medium) — a crawler must disambiguate by intent (drive a node fleet vs. mint/ship a being).
- KERNEL TAG HYGIENE: current grail release commit/version is 0.6.16, but brainstem-v0.6.16 is missing and brainstem-v0.6.15 is lightweight rather than annotated. foundation.json pins the immutable 0.6.16 commit SHA directly. Do not move legacy refs; the next release must cut a new annotated brainstem-vX.Y.Z tag and restore VERSION == latest tag.
- PORT + ROOT COLLISION: kody-w/RAPP, kody-w/rapp-installer, AND kody-w/rappterbox all install to ~/.brainstem and bind 127.0.0.1:7071. The three cannot coexist on one machine; CommunityRAPP also lands on 7071 locally (or 7072 scaffolded). Project-local RAPP uses :7072+.
- DEPRECATIONS: kody-w/rapp-installer is THE GRAIL/kernel of record; kody-w/RAPP is a downstream DISTRO that pins the unmodified kernel (rapp-distro/1.0). kody-w/RAPP_Hub is ARCHIVED (superseded by the RAR + RAPP_Store registry path). Treat rapp-installer as the kernel grail, RAPP as its reference distro, RAPP_Hub as archived.
- MISSING / 404 PROTOCOLS (gaps): (1) The Wrapped Organism Spec — RECLASSIFIED 2026-08-04: UNPUBLISHED, not 404. kody-w/rappterbook's default branch tracks zero paths matching wrapped_organism, but the spec and its reference runtime both exist on that repo's unmerged branch worktree-wrapped-organism: docs/WRAPPED_ORGANISM_SPEC.md fetches HTTP 200 (29891 bytes, sha256 9c9674bab36de2be…, pinned tip 51636f596070), alongside scripts/wrapped_organism/ and tests/test_wrapped_organism.py, added 2026-05-16 by 8969007f091c. The standalone repo kody-w/rappter also exists and is PRIVATE, not absent. So this is a merge-and-visibility gap, not a lost document, and it closes by merging the branch or opening rappter — not by hunting for a missing file. Retained here because the canonical spec is still unreachable from any default branch. (2) kody-w/wrap_leviathan ships NO code anywhere reachable — its wiring stage is aspirational; leviathan_hub `hatch` covers it today. (3) kody-w/rapp_leviathan_factory has no standalone repo (404) — the canonical artifact is a RAR agent (v0.2.0).
- SEALING COLLISION: rapp-sealed/1.0 is the CANONICAL codec (PBKDF2-SHA256 salt 'rapp-neighborhood-5a/1', 210000 iters -> AES-256-GCM). rapp-vneighborhood/1.0 uses a DIFFERENT front-door PIN-seal variant (PBKDF2-SHA256(PIN, salt 'rapp-vneighborhood:'+channel, 100000) -> AES-GCM). Two incompatible schemes; never assume a vneighborhood channel is rapp-sealed bytes.
- ALIAS, NOT A PROTOCOL: 'rapp-twin-chat/1.0' is an ALIAS for §6 of rapp-neighborhood-protocol/1.0, not a standalone spec. rapp-commons-protocol/2.0 and rapp-vneighborhood/1.0 are PROFILES/APPS on top of it; MCP is just another §5 transport carrying the same §6 envelope. None of these are new peer types.
- 'rappter*' NAMING CLUSTER: OpenRappter is the intended consumer RAPP substrate-distro and RAPP<->OpenClaw Rosetta stone (current parity incomplete; data root ~/.openrappter); rappterbox is a console re-skin on ~/.brainstem; rappter is the standalone Wrapped Organism Spec home and EXISTS but is PRIVATE (an anonymous crawl reads it as 404); the readable copy of that spec is on the kody-w/rappterbook branch worktree-wrapped-organism; rappterbook (+~25 rappterbook-*/rappter-* repos) is the social estate. They are distinct roles. OpenRappter must converge on the frozen /chat behavior and drop-in agent.py ABI without turning OpenClaw-shaped compatibility surfaces into new RAPP protocol authorities.
- 'kite' NAMING CLUSTER: rapp-kite (the operator 'string' — CDP tooling) is distinct from kite-mark (visual identity repo) and rapp-kited-twin (canonical kite-mark SVG). Three 'kite' things; only rapp-kite is the transport operator.
- RAPPID IDENTITY REPRESENTATIONAL SEAMS: multiple on-the-wire forms coexist — rappid:<birth-slug>:<64hex> (rapp-egg-hub v2.0), rappid:@owner/slug:<64hex> (canonical, now rapp/1 §6.1 — formerly attributed to the retired rapp-eternity/1.0; the grammar is byte-identical, only its owner moved), rappid:v2:<kind>:@owner/slug:<hex> (rapp-estate), rappid:v3:<base64url(SHA-256(pub))> (rapp-commons). rapp-commons' event SCHEMA and its PROTOCOL.md even show different forms. Rule (per the compatibility contract): READ all legacy forms forever, EMIT only canonical, join on the hash — never rewrite identity in place.
- IDENTITY AUTHORITY MOVED (resolved 2026-08-04): the estate-wide identity authority is rapp/1 §6 in kody-w/rapp-1, pinned at 6723c7add2aed36bb68992fc71a56b0a4bd5ad81 (SPEC.md, 41880 bytes, sha256 6d06daba65d7c045716f3d6e95db8401ab58e727820e4114466d847f62cae49b) — the same pin rapp-map/RAPP1_AUTHORITY.json records. kody-w/rapp-eternity was RETIRED to a tombstone on 2026-07-17 (commit 9c970a1e, then 17bb6d21) and is no longer a live standard, an identity authority, or a compatibility authority. The spine asserted the opposite until now because registry.json was generated 2026-07-11, six days before the retirement. What actually changed is the MINTING METHOD, not the string: §6.1 keeps rappid:@owner/slug:64hex verbatim, so emitters are fine, while §6.2 mandates mint-once and PROHIBITS deriving the tail from owner/slug or any name — so the eternity-era sha256-of-your-own-content tail is retired. RAPP-Bible remains a narrative mirror, never the owner, and its eternity narrative is now historical.
- 'DRIFT TRIANGLE' IS A MISNOMER — it has FOUR legs (the @rapp/rapp agent, rapp-god, rapp-map, RAPP-Bible) anchored on ecosystem-spec.json; when any leg disagrees with the JSON, the JSON wins. rapp-map and rapp-god both carry byte-identical mirrors of ecosystem-spec.json, so divergence between them IS the detectable drift signal.
- OVERLAP — distribution catalogs: rapp-agents (flat public agent stack + RappLoader) overlaps RAR (curated content-addressed registry); rapp-egg-hub (twin .egg hub) overlaps twin-egg-hatcher (generic hatcher) and rapp-carts (the unifying cartridge abstraction). They are complementary (stack vs registry; hub-distribution vs hatch-tool vs user-facing unit), not duplicates — but a crawler should pick by role, not by nearest match.
- OVERLAP — map renderers: RAPP-Bible (synthesized, PINNED v1.0.0 human narrative — can itself drift), rapp_docs (live raw passthrough, always-current, zero copies), and rapp-map (index of WHERE, not the TEXT) cover overlapping ground with different jobs. Use rapp_docs for current spec text, Bible for narrative onboarding, rapp-map for location.
- LICENSE COLLISION: most of the cluster is MIT (rapp-sealed, rapp-dataverse, rapp-neighborhood-protocol, rapp-kite, rapp-static-apis, rapp-agents). BUT kody-w/RAPP and rappterbox are source-available All-Rights-Reserved, and rapp-moment/1.0 is PolyForm-Noncommercial 1.0.0 + trademarked marks ('RAPP', 'Holographic Moment(s)'). A consumer/redistributor must check per-repo terms, not assume MIT.
- SECURITY GAP: the Leviathan fleet protocol's POST /api/agent/<name> route is UNAUTHENTICATED — a shell-capable agent on any body = fleet-wide RCE from one request. It is explicitly LAN/trusted-subnet only; there is no auth layer in the spec. Also the canonical fleet wire is now rapp-fleet-chat/1.0 (signed twin-chat over /chat); the legacy /api/agent route is the Phase-1 RCE.
- DANGLING IDENTITY DEFERRALS AFTER THE ETERNITY RETIREMENT (open, tracked): three live documents still defer their identity model to the retired rapp-eternity/1.0 and have not been amended by their owners. (1) kody-w/rapp-moment SPEC.md line 6 — 'Identity defers to rapp-eternity/1.0 (the sole identity standard). A Moment's rappid is the PKI-free SHA-256 content-address of its canonical body' — exactly the rule §6.2 retires; tracked upstream as kody-w/rapp-moment#2. (2) kody-w/rapp-egg-hub EGG_FAMILY.md:209 cites the 'eternity standard' for the canonical string form; lower urgency, because §6.1 preserves that form unchanged. (3) this repo's own specs/TRUST_AND_OWNERSHIP.md and specs/SUBSTRATE_TRUST.md carried the same deferral; both now open with a retirement notice and point every cross-reference, depends-on and see-also at rapp/1 §6. Their two superseded content-address minting sentences (TRUST_AND_OWNERSHIP §1 and Rule §7) are preserved verbatim and flagged inline as historical rather than rewritten, because restating a normative rule is a versioned amendment for the spec owner, not a registry refresh. One open question rides along: rapp-trust/1.0's sig_suite ladder (none → ed25519 → ecdsa-p256 → reserved-PQ) was attributed to rapp-eternity/1.0, but rapp/1 §10 defines no sig_suite field — it specifies detached-JWS alg EdDSA/ES256 instead. The ladder therefore has no upstream owner today and is recorded here rather than silently re-homed.

*Generated from `registry.json` (2026-08-04).*
